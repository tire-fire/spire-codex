# Extraction data (large, from game files)
extraction/
*.pck

# Python
venv/
__pycache__/
*.pyc
.env

# Node
frontend/node_modules/
.next/
frontend/.env.local

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Local overrides
docker-compose.override.yml

# Secrets (GitHub App private key, etc.)
.secrets/
*.pem

# Runtime data
data/runs.db
data/runs/
data-beta/runs.db
data-beta/runs/
history-of-runs/

#Analysis Docs
Spire_Codex_Growth_Strategy*
data/card_builder.db

# Card composite renderer spike — local only, do not commit
tools/card-renderer/

# webp image assets — served from the Cloudflare R2 / S3 CDN, not git.
# PNG sources stay tracked; webp is generated and uploaded to the CDN.
backend/static/images/**/*.webp
data-beta/**/*.webp
backend/static/images/cards/composite/

# Ansible — inventory.yml is rendered from inventory.yml.tpl + 1Password
# at runtime by bin/op-ansible. Origin IPs are CF-shielded; keep them
# out of git.
infrastructure/ansible/inventory.yml
